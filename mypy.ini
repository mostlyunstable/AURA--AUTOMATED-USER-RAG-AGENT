[mypy]
# NOTE: repositories/ carries pre-existing SQLAlchemy Column-typing debt
# (~224 errors, previously suppressed via the repositories.py exclusion).
# Follow-up: migrate models to Mapped[] typing, then drop this exclusion.
exclude = (?x)(
    ^core/infrastructure/database/repositories/
    | ^alembic/.*
  )
